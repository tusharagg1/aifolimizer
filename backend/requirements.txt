fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.8.2
pydantic-settings>=2.6.1
httpx>=0.27.2
yfinance>=0.2.43
pandas>=2.2.2
numpy>=1.26.0  # explicit (transitive via pandas/ta) — used directly in fama_french OLS
ta>=0.11.0
python-dotenv>=1.0.1
mcp>=1.2.0
ws-api>=0.33.0
diskcache>=5.6.0  # L2 persistent cache shared between FastAPI + MCP processes
asyncpg>=0.29.0  # TimescaleDB async driver (Phase 0)
redis>=5.0.0  # Async Redis client (Phase 0)
rq>=2.0.0  # Redis-backed task queue (Phase 14)
sentry-sdk[fastapi]>=2.0.0  # Optional error tracking (Phase 15)
pytrends>=4.9.2  # Google Trends search-interest (free, no key; optional/lazy-imported)
curl_cffi>=0.7.0  # browser-TLS impersonation for StatCan WDS (Akamai JA3 block; lazy-imported)
