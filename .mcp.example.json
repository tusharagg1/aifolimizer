{
  "_comment": "Copy to .mcp.json and replace <REPO_ROOT> with absolute path. Windows: '<REPO_ROOT>/backend/.venv/Scripts/python.exe'. macOS/Linux: '<REPO_ROOT>/backend/.venv/bin/python'. Use double-backslash on Windows JSON ('C:\\\\Users\\\\you\\\\...'). .mcp.json is gitignored.",
  "mcpServers": {
    "aifolimizer": {
      "command": "<REPO_ROOT>/backend/.venv/Scripts/python.exe",
      "args": [
        "<REPO_ROOT>/backend/mcp_server.py"
      ]
    }
  }
}
